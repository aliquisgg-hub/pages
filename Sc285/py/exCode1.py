# Code 1:

# # 左の「セルの実行」ボタン（▷）を押してみよう．
# （Jupyter Server の開始には少し時間がかかる）
# # から行末まで（緑色の文字の部分）はコメントなので，実行時には無視される．

print(1+1)  # 1+1 の計算結果を（下に）出力(print)する．
2+2         # Jupyter Notebook では，（コメント以外の）最後の「文」が値を持つ「式」の場合，その値も（下に）出力される．